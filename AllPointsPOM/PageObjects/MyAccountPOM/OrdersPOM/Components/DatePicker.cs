using CommonHelper;

namespace AllPoints.PageObjects.MyAccountPOM.OrdersPOM.Components
{
    public class DatePicker
    {
        public DomElement monthLabel = new DomElement()
        {
            locator = ""
        };

        public DomElement yearLabel = new DomElement()
        {
            locator = ""
        };

        public DomElement arrowLeft = new DomElement()
        {
            locator = ""
        };

        public DomElement arrowRight = new DomElement()
        {
        };

        public DomElement arrowRight2 = new DomElement()
        {
        };
    }
}