using Microsoft.VisualStudio.TestTools.UnitTesting;

//TODO
//Note the test data factory consumer methods needs to use different account users because each test tries to create and delete the same default user
[assembly: Parallelize(Workers = 0, Scope = ExecutionScope.ClassLevel)]
namespace FMPOnlineTests
{
    
}
